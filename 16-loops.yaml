- name: install packages
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: print names
    ansible.biultin.package:
      name: "{{ item }}"
      state: latest
    loop:
    - nginx
    - mysql
    - zip 